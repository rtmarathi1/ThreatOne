{
  "id": "FEAT-005",
  "type": "fix",
  "description": "Fix review issues: incorrect variance formula in BehaviorAnalyticsEngine, regex compiled per call in NLPProcessor, global mutex in AIEngine, unbounded concatenation in summarizeLog",
  "status": "completed",
  "steps": [
    "Fix BehaviorAnalyticsEngine::updateBaseline to use proper Welford's algorithm with M2 tracking (store m2 in UserBaseline struct)",
    "Pre-compile regex patterns in NLPProcessor at construction time and cap input size in summarizeLog",
    "Replace global mutex in AIEngine with no lock (subsystems are stateless or have their own locks)",
    "Add TODO comments for issues 5-7 (keyword-based risk factors, no concurrency tests, keyword parsing gap)"
  ],
  "acceptance_criteria": [
    "BehaviorAnalyticsEngine::updateBaseline produces correct variance using Welford's online algorithm",
    "NLPProcessor regex patterns are compiled once at construction, not per call",
    "NLPProcessor::summarizeLog caps input size to avoid unbounded memory usage",
    "AIEngine does not serialize independent operations behind a single mutex",
    "Build and all tests pass"
  ],
  "verification": [
    "cd /projects/sandbox/ThreatOne && cmake -B build -DENABLE_QT=OFF && cmake --build build && cd build && ctest --output-on-failure"
  ],
  "blocked_reason": null,
  "findings": "All 4 concrete review issues fixed. Build and all tests pass. Welford's M2 is now tracked in UserBaseline struct alongside hourStdDev. NLPProcessor compiles 4 default regex patterns plus the IP regex at construction time. summarizeLog caps combined input at 1MB. AIEngine no longer holds a global mutex since FeatureExtractor/ThreatClassifier/RiskScoringEngine/NLPProcessor/RecommendationEngine/AutoInvestigation/PredictionEngine are stateless, and AnomalyDetector has its own internal mutex."
}
